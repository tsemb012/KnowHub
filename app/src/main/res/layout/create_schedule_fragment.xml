<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.droidsoftthird.CreateScheduleViewModel" />

    </data>

<FrameLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CreateScheduleFragment">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


    <TextView
        android:id="@+id/yearAndMonth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blue_first"
        android:fontFamily="sans-serif-medium"
        android:paddingStart="20dp"
        android:paddingTop="12dp"
        android:paddingEnd="20dp"
        android:paddingBottom="12dp"
        android:textAllCaps="true"
        android:textColor="@color/black_first"
        android:textSize="14sp"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="MMM YYYY" />

    <com.kizitonwose.calendarview.CalendarView
        android:id="@+id/calendar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cv_dayViewResource="@layout/calendar_day_layout"
        app:cv_monthHeaderResource="@layout/calendar_header"
        app:cv_orientation="horizontal"
        app:cv_scrollMode="paged"
        app:layout_constraintTop_toBottomOf="@id/yearAndMonth"/>

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/calendar">

        <include
            android:id="@+id/include_title"
            layout="@layout/include_item_schedule_editable"
            bind:image="@{@drawable/ic_baseline_text_fields_24}"

            />

        <include
            android:id="@+id/include_date"
            layout="@layout/include_item_schedule"
            bind:image="@{@drawable/ic_baseline_date_range_24}"
            bind:text="@{viewModel.}"
            />

        <include
            android:id="@+id/include_time"
            layout="@layout/include_item_schedule"
            bind:image="@{@drawable/ic_baseline_access_time_24}"
            bind:text="@{viewModel.}"
            />

        <include
            android:id="@+id/include_location"
            layout="@layout/include_item_schedule"
            bind:image="@{@drawable/ic_baseline_location_on_24}"
            bind:text="@{viewModel.}"
            />

        <include
            android:id="@+id/include_group"
            layout="@layout/include_item_schedule"
            bind:image="@{@drawable/ic_baseline_group_24}"
            bind:text="@{viewModel.}"
            />

        <include
            android:id="@+id/include_repeat"
            layout="@layout/include_item_schedule_checkable"
            bind:image="@{@drawable/ic_baseline_repeat_24}"
            bind:text="@{viewModel.}"
            />


    </LinearLayout>

        <Button
            android:id="@+id/btn_reset"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> ViewModel.resetSchedule()}"
            android:text="@string/reset"
            app:layout_constraintEnd_toStartOf="@id/btn_register"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/linearLayout1" />

        <Button
            android:id="@+id/btn_register"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:enabled="@{viewModel.enableState}"
            android:onClick="@{() -> viewModel.createSchedule()}"
            android:text="@string/register"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/btn_reset"
            app:layout_constraintTop_toBottomOf="@+id/linearLayout1" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
</FrameLayout>
</layout>